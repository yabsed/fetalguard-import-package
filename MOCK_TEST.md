# 반출 심사용 진단 확장 검증 — 2026-09-17

변경 관련 테스트 **53개 통과**: export 계열 30개, 방문 진단 8개, 실행 배치 7개, 현장 그림 8개. 집계 선별, 작은 산모 집단 억제, 내부 경로·식별자·외부 링크 차단, 원자적 생성, 별도 manifest, 진단 생성 실패 시 실행 실패 표시를 확인했다.

기존 `full-84c5396148cb2c60`에 재학습 없이 `export_review/visit_audit/`를 생성했다. CSV 24개·PNG 181개를 포함한 총 209개 파일이며, 학습 작업 243개의 반복 이력 57,200행을 포함한다. 전체 실행 검증에서 기존 단계 해시 일치, 산모 누수 없음, 공통 test 유지, 지표 254쌍 독립 재계산 일치를 확인했다.

새 실행에서는 반출 심사 후보인 집계·학습·운영 진단을 `export_review/visit_audit/`에 자동 생성한다. 이미지 후보는 `export_review/images/`와 `export_review/visit_audit/images/`, 집계 CSV 후보는 각 `csv/`에서 확인한다. 아래의 내부 전용 진단 설명은 확장 전 검증 이력이며, 현재 출력 계약은 `OUTPUTS.md`를 따른다.

# 첫 방문 진단 검증 — 2026-09-17

Python 3.13.11에서 전체 테스트 94개 중 **93개 통과**, Jupyter 폼 테스트 1개는 해당 로컬 환경의 `ipywidgets` 미설치로 실행 오류가 났다. 의존성을 자동 설치하거나 테스트를 통과한 것으로 처리하지 않았다. 새 테스트는 비정상 JSON·입력 변종을 조사 후 보존하는지, 보간 전 0/평탄/비정상 값을 그대로 세는지, 파일 연결 분모·중복을 구분하는지, 사전검사 실패에도 일지/보고서가 남는지, 조사 전용이 학습/GPU 검사를 호출하지 않는지, 상한·patience·불완전/미수집 상태를 구별하는지 확인한다. 실제 소형 CatBoost/XGBoost 학습의 반복 이력·모델 재사용, 기존 CNN 중단/재개의 가중치 완전 일치, 진단 갱신 후 현장 그림의 해시 검증도 통과했다.

로컬 원천 전량을 `--survey-only`로 조사했고, 별도로 `/usr/bin/python3 -S`로 site-packages 없이 같은 조사를 완료했다. 원천 annotation 고유 ID 2,301개, 라벨만 있는 ID 35개, 서로 다른 ID의 동일 원천 신호 표현 13그룹을 확인했다. 13그룹은 쌍태아 원본 공유일 수 있으며 누수 판정이 아니다. 원천 구간 22,516개는 중복 배포/두 태아를 포함하므로 최종 학습의 9,877개와 분모가 다르다. `--check --profile full --device cpu`로 공식 모델·환경 사전검사와 엄격한 입력 발견도 통과했다. **full GPU 학습은 다시 실행하지 않았다.**

기존 `full-c0959e89e453af6a`를 읽어 별도 `.scratch/first-visit-audit-check/full-gpu-review-v2/`에 진단을 생성했다. CNN 30회 중 29회 patience 종료, 1회 상한 도달/최고점 이후 139회로 patience=150 미충족을 재현했다. 트리 243개는 이전 코드가 이력을 남기지 않아 미수집으로 표시했다. 기존 모델·예측·심사 묶음은 수정하지 않았다. 생성 HTML을 로컬 브라우저로 렌더링해 한글·표·그래프 배치도 확인했다.

산모 수 learning curve, 추가 학습 예산 비교, 검증된 시각 기반 분석은 이번에 수행/구현한 것으로 표시하지 않는다. 조사·로그·진단은 내부 전용이며 반출 승인 자료가 아니다.

# 출력 schema 3 검증 — 2026-09-17

새 실행은 집계 CSV를 `export_review/csv/`, 현장 그래프를 `export_review/onsite_figures/`에 생성한다. 현장 그림은 별도 manifest로 검증하고 반출 선별 목록에 포함하지 않는다.

Python 3.13.11에서 변경 관련 테스트 **35개**를 통과했다: `test_export_review` 17개, `test_export_images` 6개, `test_onsite_figures` 8개, `test_run_layout` 4개. CSV 위치·링크, 현장 자료의 별도 검증, 원자적 생성, 게시 직후 중단 복구, 중첩 디렉터리의 단계별 재개/변조 탐지를 확인했다. 기존 schema 2 로컬 full 결과도 읽기 전용으로 검증해 615개 파일의 해시 일치를 확인했다. 이번 출력 개편으로 전체 학습을 다시 실행한 것은 아니다.

아래는 기존 분석 구현의 검증 이력이다. 당시의 전체 mock/환경 검증과 이번 출력 변경의 테스트 범위를 구분한다.

# 설계 2.0 검증 기록 — 2026-09-17

이번 개편의 전량 mock을 Python 3.10.20 / PyTorch 2.4.1 CPU, 4 threads에서 완료했다. 입력은 기존 2,301건·2,288명·9,877개 구간이며 반입 폴더에 데이터를 복사하지 않았다. 첫 통합 실행은 사전검사를 포함해 약 2분 10초였다. 2만 건의 full 실행 시간이나 새 설계의 full 성능을 측정한 값은 아니다.

당시 코드의 단위·합성 테스트 **67개**가 같은 Python 3.10 환경에서 통과했다. 현장 사례 파형·28개 입력 인자 정렬, SHAP 미산출 사례의 인자 표시, 실제 SHAP 표본 분모 선별, CV·seed 보조표의 경보 지표 우회 차단, EMR 선택 표, 아웃컴 결측 집단의 관측 분모, mock 가설표의 `execution_test_only` 표시를 포함한다.

## 새로 검증한 계약

- 실험 A 14개 선택 결과(Cat18·Cat28·XGB·로지스틱·절제·무평활·선형/비선형 단일 인자)와 CNN 4개 결과가 같은 test를 평가한다.
- 선형 단일 인자 28개와 비선형 spline 28개가 같은 CV 분할을 사용한다. 합성 U자형 자료에서는 선형이 놓치는 관계를 spline이 학습하는 것을 확인했다.
- 평활/무평활 팔은 구간 ID로 정렬하며 누락·중복·라벨 불일치를 거부한다.
- CNN 정규화는 같은 width·seed로 비교하며, 선택은 validation seed 평균으로 한다. EMR 유무도 seed 후보와 선택 예산이 같다.
- 기존 분석 산모는 주 holdout에서 train에만 배정한다. 실제 자료의 기존 노출 배제는 승인된 호환 산모 목록을 제공해야 검증된다.
- 연속 판독 길이는 기록 접두어·구간 번호가 끊어지면 재설정하고, 확인되지 않는 순서는 결측으로 남긴다.
- 산모 누수 없음, 신호와 라벨·모델 평가 대상 일치, 모든 완료 단계 SHA256 일치를 확인했다.
- 첫 통합 실행의 AUROC/AP 122쌍을 sklearn으로 독립 재계산해 일치를 확인했다.
- `internal/`에 개인별 자료·모델·상세 오류가 모이고, `export_review/`는 허용 필드의 집계만 다시 생성한다.
- 반출 심사 묶음에 식별자·경로 주입, 임의 모델 JSON·외부 링크·심볼릭 링크·작거나 미확인된 산모 집단을 넣는 합성 시험을 통과했다. 파일 변조·미허용 파일을 검증기가 탐지한다.
- 중단된 심사 묶음은 외부 후보 경로에 부분 공개되지 않으며, 생성 완료 직후 중단되어도 무결성을 확인해 재개한다.
- `--check`는 최근 분석 `LATEST.json`을 덮어쓰지 않고, 실패 상세 traceback은 내부에만 남는다.

## 재검증 명령

```bash
python -B -m unittest discover -s tests -v
# 저장소 루트에서 실제 데이터 전량으로 새 mock
FETALGUARD_PYTHON=/사용할/python bash 본선/02-반입할-파일/MOCK.sh --device cpu
python -B 본선/02-반입할-파일/tools/validate_run.py /결과/mock-실행해시 --expect-records 2301 --check-original-xgb
```

현장에서는 `internal/report/report.html`과 사례 검토 화면을 확인한다. 심사 준비는 `export_review/report.html`에서 하고, 이미지 전용 심사에는 `export_review/images/`의 PNG만 선택한다. 자동 선별 통과와 기관 반출 승인은 다르다. mock은 실행·계약 검사이며 낮은 학습 예산의 점수나 CI를 연구 결론에 사용하지 않는다.

## 이미지 반출 묶음 검증

```bash
python -B -m unittest discover -s tests -p 'test_export*.py' -v
```

`test_export_images.py`는 실제 산모 자료 없이 합성 집계만 사용한다. 28개 고정 집계 영역의 그래프 생성, 모든 인자 반응 곡선, 후보 목록 페이지 분할, CI 끝점과 0/억제/미산출 구별, 내부 전용 자료 거부, CSV·PNG 메타데이터·추가 페이로드 거부, 기존 실행 재사용 시 선별 기준 유지/상향을 검사한다. `test_export_review.py`는 기존 소수 집단·분모 선별과 원자적 생성·해시·외부 링크 차단을 함께 검사한다.

PNG만 생성하는 부분은 모델 재학습이나 신규 패키지 설치를 요구하지 않는다. 기존 실행을 이용할 때는 `tools/build_image_review.py`를 사용한다. 실제 기관 데이터의 수치와 전체 학습 재실행은 합성 테스트로 대체되지 않는다.

아래는 개편 전 1.0의 검증 이력이다. 이전 full 결과는 그대로 보존했으며, 버전 2.0 full 결과로 이름을 바꾸거나 재사용하지 않았다.

# 1.0 로컬 검증 기록 — 2026-09-17

실데이터를 반입 폴더에 복사하지 않고, 저장소의 `dataset/korean-ctg/dataset`을 외부 입력으로 지정했다. mock도 데이터는 전량 읽으며 학습 예산만 줄인다.

## 실행 환경

| 항목 | 현장 버전 계열 검증 |
|---|---|
| Python | 3.10.20 (신청한 3.10.13과 패치 버전 차이) |
| numpy / pandas / scipy | 2.2.6 / 2.3.3 / 1.15.3 |
| scikit-learn | 1.7.2 |
| CatBoost / XGBoost | 1.2.10 / 3.2.0 |
| PyTorch | 2.4.1+cpu |
| OpenCV / Matplotlib | 4.10.0 / 3.10.8 |
| 실행 장치 | CPU, 4 threads |

별도로 기존 로컬 Python 3.13.11 / torch 2.11 / XGBoost 3.4 환경에서도 전체 mock을 완료했다. 환경이 다르면 실제 CV 분할·학습 수치가 달라질 수 있으며 `run_manifest.json`에 환경을 저장하고 서로 다른 실행 ID를 부여한다. 모든 하위 의존성까지 현장 신청 lock과 동일한 환경이거나 현장 Ubuntu/GPU 자체를 검증한 것은 아니다.

## 실제 전량 mock 결과

| 검증 항목 | 결과 |
|---|---|
| 중복 제거 후 수치 신호 레코드 | 2,301건 |
| 산모 | 2,288명 |
| 쌍둥이 채널 선택 후 5분 세그먼트 | 9,877개 |
| 양성 세그먼트 | 1,771개 |
| Bbox 검사 | 715개 기록, 불일치 0건 |
| 신호 없는 탐지 전용 기록 | 35건을 주 분석에서 제외하고 명시 |
| 실험 A | 트리 2종, 6그룹 절제, 강건성 부분집합, 단일 인자 28개, SHAP 완료 |
| 실험 B | CNN 4회 학습, 작은/중형 × 정규화 2종 비교 완료 |
| 부가 분석 | pH·Apgar 2종, FIGO 항목 7종, 기관 12곳 LOSO, EMR 추가 및 하위군 완료 |
| 공식 YOLO | PNG 715장 전량 추론, 공통 홀드아웃 부분집합 비교 완료 |
| 공식 XGBoost | 원 파서 재현 정확도 1,978 / 2,301 = 0.8596262494567579 (기존 0.860 재현) |
| 보고서 | HTML·Markdown·CSV·PNG·PDF 생성 |
| 결과 무결성 | 전 단계 파일 SHA256 일치 |
| 데이터 분할 | 산모 누수 없음, 주 모델들 동일 테스트 세그먼트 |

현장 버전 계열 mock은 사전검사부터 보고서까지 약 100초였다. 전체 예산의 학습 시간이나 2만 건 처리 시간을 실측한 값이 아니다.

## 23개 단위 테스트

- 산모 그룹 분할, 다태아 채널 선택, 라벨 길이 불일치 거부
- 중복 파일 제거 및 상충하는 라벨/Bbox 거부
- 실제 PNG 치수 기반 Bbox 검사, 이미지 부재를 미검증으로 처리
- 0.5Hz에서 15초 감속 경계의 올림 처리, 28개 인자·그룹 정합성
- CNN 학습셋만의 스케일 산출, TOCO의 0/음수 보존
- 동점·산모 가중치가 있는 AUROC/AUPRC/Brier를 sklearn과 대조
- 동일 예측의 paired 차이 CI=0, 정상 기록 경보율·시간당 분모, 단일 클래스 상태
- 공식 YOLO NMS 점수·우측 경계 세그먼트 대응
- 완료 단계 재사용, 완료 산출물·승인 코드 변조 감지, 실패 단계 성공표지 방지
- CNN을 1 epoch 후 중단하고 재개했을 때, 연속 학습한 모델과 **모든 가중치 완전 일치**
- Jupyter 입력 폼 생성, 런타임 네트워크 연결 차단

```bash
python -B -m unittest discover -s tests -v
python -B tools/validate_run.py /결과/mock-실행해시 --expect-records 2301 --check-original-xgb
```

이 테스트들은 실행과 분석 계약을 확인한다. 3 epoch / 50 bootstrap의 mock 성능은 본선 연구 결과로 사용하지 않는다. 안심존 원본 전량과 GPU 실행은 현장에서 full 모드로 수행해야 한다.
