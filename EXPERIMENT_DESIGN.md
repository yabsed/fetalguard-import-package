# 분석 설계 2.0

이번 설계는 로컬 2,301건 파일럿에서 발견한 비교·측정 문제를 반영한 것이다. 파일럿 결과를 보고 정한 변경이며, 파일럿을 새 가설의 독립 확증 데이터로 부르지 않는다. 본선 결과를 보기 전에 이 버전과 설정을 고정한다. `internal/protocol.json`에 실행 전 규약과 예산을 저장한다.

연구 순서는 **측정 타당도 → 판독 예측 → 단독과 조합 → 기여 구조 → CNN과의 성능·비용 비교 → 사이트 전이 → 아웃컴 대응**이다. 목표는 전문의 Abnormality 판독의 예측 구조이며, 이상 발생 원인이나 전문의 인지 과정의 인과적 규명이 아니다.

## 공통 평가 계약

- 분석 대상: 지정한 디렉토리의 전량 데이터. 태아 선택·산모 ID·5분 150표본·라벨 정렬은 기존 검증을 유지한다.
- 예측 단위는 선택 태아의 5분 구간, 분할·부트스트랩 단위는 산모다. 세그먼트를 독립 산모 수처럼 세지 않는다.
- 기본 train/validation/test는 약 80/10/10이다. CV는 test를 제외한 개발 코호트 안에서만 하며, 각 fold 안에 조기중단·임계값용 검증셋을 둔다.
- `--prior-cohort`를 주면 이미 분석한 산모와 그 산모의 모든 기록은 **주 holdout 분할에서** train에만 배정한다. 나머지 산모에서 validation/test를 만든다. 목록이 없으면 `not_checked`이며, 새로 보지 않은 평가 코호트라는 보장은 하지 않는다. ID 체계가 바뀌면 이 매칭으로 이전 노출을 배제할 수 없다. 개발 CV·H5·LOSO는 기존 산모도 포함하는 탐색 분석으로 유지한다.
- H1/H4의 주 성능 지표는 AP(`auprc` 저장 필드), AUROC와 검증셋 특이도 90% 임계값의 운영 성능은 함께 보고한다. **H2의 주 지표는 AUROC**이며, 단일 인자 선택도 제안서대로 검증 AUROC를 사용한다. H2의 AP 차이는 부차 결과다.
- 테스트셋은 모든 선택 이후 평가한다. 점수가 마음에 들 때까지 선택 규칙·특성·임계값을 바꾸지 않는다.
- CI는 산모 클러스터 부트스트랩 2,000회(full)다. 같은 평가 산모에서 두 예측의 차이를 계산한다. 학습·모델 선택 과정 전체의 불확실성을 포함하는 CI는 아니다.
- 여러 절제·사이트·아웃컴 비교는 탐색적이다. 유의한 항목만 골라 주 가설로 승격하지 않는다. 비열등성 한계는 정하지 않았으므로 동등성/비열등성을 자동 선언하지 않는다.

## 측정: 인자 28개가 무엇을 측정하는가

`data/feature_quality.csv`에 인자별 상수 여부·0 비율·산모 수·정의 제약을 기록한다. `n_severe_decel`은 지속 >5분을 요구하는 규칙이므로 5분 창에서 **구조적으로 측정 불가**다. 상수 0을 임상 사건의 부재로 해석하지 않는다. `stv`는 0.5 Hz 신호의 표본 간 변화량이며 임상 박동 간 STV가 아니다.

주 분석은 기존 30초 평활을 유지한다. 민감도 팔은 같은 결측 보간·동일 환자·동일 특성 규칙에서 평활만 제거한다. `segments_unsmoothed.csv`와 `preprocessing_sensitivity.csv`에 이를 기록한다. TOCO=0 해석과 구간 길이 제약은 두 팔에 공통으로 남는다. 전문의 기록 단위 FIGO 주석은 구간 정답으로 복제하지 않는다.

`longest_abnormal_run`은 파일명 접두어가 바뀌거나 번호가 +1로 이어지지 않으면 끊는다. 파일명 규칙이 확인되지 않으면 결측이다. 연속 번호도 실제 타임스탬프 검증을 대체하지는 않는다.

## H1–H3: 비교 사다리

| 비교 | 달라지는 것 | 답하는 질문 |
|---|---|---|
| 단일 선형 vs 단일 스플라인 로지스틱 | 하나의 인자 안에서 허용하는 비선형성 | 선형 대조군이 U자형 관계를 놓쳤는가? |
| 검증셋에서 선택한 최선 단일 비선형 vs Cat28 | 인자 조합과 모델 표현력 | 이번 제한된 단일 모델군보다 조합 모델이 좋은가? |
| 전체 인자 로지스틱 vs Cat28 | 같은 28개 입력의 학습 방식 | 단순 가산 모델로 어느 정도 설명되는가? |
| Cat18 vs Cat28 | 추가 인자 10개 | 같은 전처리에서 확장 인자가 정보를 더하는가? |
| Cat28 vs 6개 특성군 제거·robust | 입력 특성군 | 다른 특성이 있는 상태에서 추가 기여가 남는가? |
| Cat28 vs cat28_unsmoothed | 30초 평활만 | 기여와 성능이 전처리에 민감한가? |

단일 비선형 모델은 train에서만 구한 범위의 균일 knot 5개·3차 spline과 정규화 로지스틱(C=1)으로 고정한다. 상수 인자는 절편 모델로 처리한다. 28개 중 선택은 validation AUROC, test는 선택 이후다. 이것도 모든 가능한 단변량 모델의 최적 성능은 아니다.

CatBoost 설정과 seed 후보는 인자 팔들에서 같다. 모델별 validation AP로 선택하되, 같은 seed끼리의 절제 차이도 별도 저장해 선택 잡음과 방향의 일관성을 확인한다. `feature_response_train.csv`는 훈련셋 값 구간별 이상 비율·산모 수를 담는다. 이는 기술적 연관성이지 인과 효과가 아니다. SHAP와 개별 값은 내부 검토용, 평균 절댓값 중요도와 그룹 제거 차이는 집계용이다. 상관된 특성의 대체 가능성과 측정 한계를 함께 해석한다.

현장 사례 화면은 Cat28 테스트의 TP/TN/FP/FN별 최대 2개를 고정 규칙으로 선택한다. TP·FP는 높은 점수, TN·FN은 낮은 점수 순이고 동점은 기록 ID·구간 번호로 정한다. SHAP가 없는 사례도 제외하지 않는다. 전체 오류 분포의 대표 표본이나 전문가 설명 평가 실험은 아니며, 사례를 본 뒤 테스트에 맞춰 모델·임계값을 재선정하지 않는다. 개별 파형·식별자·기여는 내부에만 저장한다.

## H4: CNN 비교

- 파형만의 2×150 입력과 학습셋에서 구한 channel max-abs 정규화를 주 대조로 유지한다.
- 작은/중형 그룹마다 seed별 validation AP **평균**으로 width를 선택하고, 해당 width 안에서 validation AP 최고 seed를 선택한다. 테스트 점수로 width/seed를 고르지 않는다.
- 정규화 절제는 max-abs 검증 성능으로 정한 공통 width에서 **같은 seed끼리** 비교한다. 정규화마다 따로 선택한 모델의 차이와 이 짝지은 비교는 별도 결과다.
- 용량 탐색 결과, 상한 도달 여부, epoch, 파라미터 수, 추론 비용을 기록한다. 조기중단만으로 최적 수렴을 입증하지 않는다.
- 특징 행렬에 대한 트리 추론과 파형에 대한 CNN 추론의 비용 범위를 명시한다. 인자 추출의 평균 처리 시간은 별도 기록하므로 end-to-end 비용을 숨기지 않는다. 배치 처리 벤치마크를 단일 환자의 임상 지연시간으로 해석하지 않는다.
- 차이의 CI가 0을 포함하면 방향을 확정하지 못한 것이다. `H4 지지`, `동등성 입증`으로 자동 바꾸지 않는다.

## 사이트·EMR·H5

LOSO는 해당 사이트 산모를 source 학습·보정·임계값 선정에서 모두 제외한다. 사이트별 AP와 함께 기저율, 특이도, 민감도, 정상 기록 경보율 및 CI를 제시한다. 코드의 site는 실제 병원과 일대일이라고 확인된 식별자가 아니다. 현재 LOSO는 Cat28의 내부 사이트 전이이며, CNN의 외부병원 일반화를 증명하지 않는다.

Source-only Platt는 확률 보정이다. 단조 변환 뒤 같은 검증 음성 분위수로 임계값을 다시 잡으면 경보 판정은 대개 동일하다. 확률 보정 효과와 오경보 감소 효과를 혼동하지 않는다. 정상 시간당 경보는 **양성 5분 창 수**이며 연속 경보 episode 수가 아니다.

EMR 추가 실험은 같은 train/val/test·같은 seed 후보·같은 학습 예산·같은 선택 규칙을 사용한다. 선택 모델끼리의 비교와 같은 seed끼리의 차이를 둘 다 남긴다. 분만 시점 재태주수의 실제 모니터링 시점 가용성은 확인되지 않았으므로 전향적 배포 효용으로 확장하지 않는다.

H5는 전문의의 실제 판독 요약을 이용한다. 모델의 예측 판독 점수를 아웃컴 예측에 넣은 분석이 아니다. (1) 판독-아웃컴 교차표·상관과 (2) 임상정보 위에 판독 요약을 추가한 OOF 증분을 따로 보여준다. 아웃컴 관측/결측 집단의 규모와 판독 구성을 함께 남긴다. 증분 CI가 0을 포함하는 것은 무관함의 증거가 아니다. 판독자 일치도·분만까지 시간은 제공되지 않으면 추정해서 만들지 않는다.

## 이번 개편에서 하지 않은 주장

로컬 샘플을 사용한 mock은 연결·계약 검증이다. 새 설계의 full 성능은 새 full 실행 전에는 존재하지 않는다. 2만 건에서 모델 우열이 유지된다는 주장이나, 임상적 이득·외부병원 검증·반출 승인도 자동 생성하지 않는다. 학습량별 성능 곡선과 전문가의 설명 평가 연구는 이번 기본 파이프라인에 포함하지 않았다.
