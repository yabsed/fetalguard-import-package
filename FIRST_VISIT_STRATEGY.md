# 첫 방문에서 다음 방문의 설계 근거를 확보하기

첫 방문의 성공 조건은 모델 실행 완료와 함께 **두 번째 방문의 실행 계획을 증거로 결정할 수 있는 것**이다. 기존 H1–H5 실험은 유지한다. 그 위에 데이터 조사, 실행 진단, 관측에서 의사결정으로 이어지는 기록을 추가하는 방향을 제안한다.

이 문서는 첫 방문의 설계 근거와 후속 제안이다. 이번 버전은 아래 P0 중 원천 조사·지속 일지/자원 표본·학습 종료 진단·다음 방문 준비표를 구현했다. 원래 실험·분할·예산은 유지한다. 아래의 '현재 확보되는 자료'는 개선 전 기준이며, 새 구현 범위와 남은 작업은 다음 절을 따른다.

## 구현된 P0와 남은 범위

- `fg/survey.py`: 엄격 파서 전에 원천 파일·필드·연결·신호 품질·형식 변종 조사. `--survey-only`로 학습 환경 검사 없이 실행 가능. 파일 오류를 누적하며 조사한다. 원천 통계와 최종 학습 표본의 분모를 구분한다.
- `fg/telemetry.py`: 실행 시도별 console/events/resources 일지. 사전검사 실패에도 보존하고, 단계·트리 fit·CNN epoch·재개·해시 소요 시간을 기록한다. CPU/RSS 및 장치 전체 GPU/VRAM/디스크를 주기 표본으로 수집한다. SIGKILL/전원 차단은 최종 요약을 보장하지 못한다.
- `fg/cnn.py`, `fg/models.py`: 기존 수치 학습 절차를 유지하며 CNN 학습률/epoch 시간과 트리 실제 검증 이력을 추가한다. 트리는 완료 fit에서 이력을 저장하므로 중간 종료의 전체 반복 곡선까지 보존하지는 않는다.
- `fg/visit_audit.py`: 내부 HTML/SVG 데이터 지도·종료 분류·학습 곡선·단계 비용, `training_diagnostics.csv`, `next_visit_plan.csv`, 기관 답변용 `questions.md`. `export_review/onsite_figures`에서 연결하지만 반출 집계에 넣지 않는다. 기존 결과는 `tools/build_visit_audit.py`로 재학습 없이 읽는다.
- **미구현/미확인:** 산모 수 learning curve, 추가 예산 실험 자동화, 검증된 측정 시각 기반 기간 분석, 임상적 단위/판독 합의/시점 가용성 확인. 값 분포만으로 이를 추정하지 않으며 준비표에 다음 행동으로 남긴다.

## 1. 이미 남는 정보와 부족한 정보

| 질문 | 현재 확보되는 자료 (`internal/` 기준) | 첫 방문에 더 필요한 정보 | 두 번째 방문에서 바뀔 결정 |
|---|---|---|---|
| 실제 사용할 수 있는 표본은 얼마나 되나? | `data/summary.json`, `site_inventory.csv`, `exclusions.csv`, 산모 단위 분할 | 파일 → 신호/라벨/EMR 연결 → 파싱 → 사용 가능 구간의 흐름; 기관·기간별 탈락 사유와 분모 | 기관별 예산, 분석 대상, 부족한 클래스 확보 |
| 현장 자료가 로컬 입력 계약과 같은가? | 입력 파일 해시, 채널·2초 간격·길이 계약 검사; 위반 시 중단 | 학습 전에 읽기 전용으로 구조·키·타입·단위·파형 길이·표본 간격의 변종과 파싱 실패 유형을 집계 | 다음에 반입할 어댑터·전처리 코드 |
| 파형은 어떤 상태인가? | 구간 결측 기준, 28개 인자의 상수/0 비율, 평활 민감도 | 보간 **전** 결측 구간 길이, 평탄 신호, 클리핑·비정상 값, FHR/TOCO 동기화, 기관/장비별 품질 차이 | 품질 기준, 신호 처리, 제외 민감도 분석 |
| 한 사람의 여러 기록은 어떻게 연결되는가? | `mother_id` 분할, twins, 동일 파일 중복, 파일 인덱스 기반 연속성 대용치 | 다른 ID의 중복 파형, 같은 산모의 재방문, 날짜·시각·원 기록 연결의 신뢰도 | 누수 방지 단위, 시간 문맥 모델의 가능 여부 |
| 라벨과 EMR이 무엇을 의미하는가? | 판독 문자열·bbox 정합성, 기록 단위 FIGO 비교, EMR 결측 | 판독자 수·합의/수정 과정, 필드의 0/빈칸/기본값 의미, 수집 시점, 변수의 실제 예측 시점 가용성 | 예측 과제 정의, 사용할 변수, 필요한 기관 문의 |
| 기관·기간·아웃컴이 고르게 관측됐는가? | LOSO, 기관별 성능, 아웃컴 결측·관측집단 요약 | 기관×기간×아웃컴 가용성, 클래스별 산모 수, 관측/결측 집단 차이; 날짜가 없으면 불가 명시 | 외부/시간 검증 가능성, 분석 가능한 아웃컴, 기관별 해석 범위 |
| 학습이 왜 끝났는가? | CNN `models/*/history.csv`, `complete.json`, `capacity_validation.csv` | CNN·트리 모두 중단 사유, 마지막 개선 이후 횟수, 최근 검증 변화, train/validation 곡선, 실제 학습률 | 학습 연장, 조기 종료 설정, 모델 용량/정규화 실험 |
| 현장 시간 안에 무엇을 할 수 있나? | 완료 단계의 `.state/*.json` 소요 시간, 환경 버전, 일부 추론·실행 비용 | 단계/작업/epoch 시작·종료·오류·재개 기록, 주기적 heartbeat, CPU/RAM/GPU/VRAM/디스크 사용량, 해시·I/O 시간 | 병렬화, 배치 크기, 후보 수, 방문 시간표 |

중요한 빈틈: 현재 학습 파서는 계약에 맞지 않으면 실패한다. 첫 방문에서는 실패 자체도 자료의 특성을 알려주는 관측이다. 그래서 조사 도구는 학습 파서보다 먼저 실행하고, 지원하지 않는 구조를 집계해 조사 보고서를 완성해야 한다. 학습에서는 계약을 계속 엄격히 지킨다. 알 수 없는 값을 조용히 보정하거나 제거해서 실행 성공률만 높이지 않는다.

필드가 없어서 계산하지 못한 것, 프로그램 오류, 분석 비활성화, 표본 부족, 선별 억제는 각각 다른 상태로 남겨야 한다. 특히 ID와 시각·장비 메타데이터의 의미는 값의 분포만으로 확정할 수 없으므로 담당자에게 확인한 답변과 미확인 질문을 함께 기록한다.

## 2. 첫 방문의 핵심 산출물 네 가지

1. **데이터 지도**: 원본/사용 가능 산모·기록·구간 규모, 연결 성공률, 구조 변종, 결측·제외·클래스·기관/기간 분포. 모든 비율에 분석 단위와 분모를 표시한다. 조사만으로 확인할 수 없는 사항은 미확인으로 둔다.
2. **실행 일지**: 설정·코드·환경·입력 버전, 단계/모델/seed/fold/epoch별 상태와 시간, 실패 사유, 중단·재개, 자원 사용. `print`만 하지 말고 내부 `events.jsonl`과 사람이 읽는 요약으로 지속 저장한다. 과정이 실패해도 일지를 남긴다.
3. **학습 예산 진단**: 각 후보의 train loss, validation AP, 최고 epoch, 실제 epoch, 학습률과 중단 원인을 같은 화면에 표시한다. 데이터 양에 따른 성능은 개발 코호트의 산모 단위 learning curve로 별도 측정한다.
4. **다음 방문 의사결정표**: 관측 → 가능한 설명 → 이를 구별할 실험 → 바꿀 한 요소 → 예상 시간·필수 입력 → 성공/중단 기준 → 우선순위. 첫 방문에 얻은 근거 파일과 연결한다.

권장 후속 구현은 원본/진단 로그를 `internal/visit_audit/`에 남기고, 읽기 쉬운 요약을 `export_review/onsite_figures/`에서 연결하는 것이다. 외부에서 2주 동안 사용할 집계 자료는 별도 선별/심사를 거쳐야 한다. 현장 로그를 통째로 반출할 수 있다고 전제하지 않는다. 재방문 때 재개할 내부 모델·체크포인트·ID 연결표의 보존 여부도 퇴실 전에 확인한다.

## 3. saturation은 세 가지 질문으로 나눈다

- **학습 시간**: 같은 모델을 더 최적화하면 validation이 개선되는가? train loss만 내려가는지, 검증 최고점이 끝부분에 있는지, 최근 개선이 없는지 확인한다.
- **데이터 양**: 같은 예산에서 산모 수를 늘리면 검증 성능이 개선되는가? 개발 데이터의 25/50/75/100% 등으로 산모·기관 분포를 고려한 반복 learning curve를 만든다. 표본 크기와 클래스 가용성을 함께 표시한다.
- **표현/용량/라벨의 제약**: 충분히 학습해도 모델 크기, 입력 창, 라벨 품질 등의 제약이 남는가? 작은/큰 모델의 train–validation 차이, seed 변동, 오류 사례와 기관 차이를 함께 본다.

`hit_cap=false`나 early stopping은 수렴 증명이 아니다. `hit_cap=true` 역시 예산 부족을 입증하지 않는다. 예산 상한, patience, 최고점 위치와 최근 곡선을 같이 보고 **연장 확인 후보**로 분류해야 한다. 새 트리 코드는 실제 학습 반복 수와 validation 이력을 공통 진단표에 연결한다. 이전 실행에 이력이 없으면 수렴을 추정하지 않고 미수집으로 표시한다.

현 CNN은 `CosineAnnealingLR(T_max=cnn_epochs)`를 사용한다. epoch 상한을 바꾸면 학습률 일정도 달라진다. 동일 일정의 체크포인트 연장과, 새 일정으로 처음부터 학습하는 비교를 구분해서 설계해야 한다. 설정이 바뀌면 실행 ID도 달라지므로 단순 재실행을 기존 학습의 연장으로 간주하지 않는다. 테스트셋으로 학습 연장 여부를 고르지 않는다.

### 현재 로컬 실행에서 확인한 예시

`full-c0959e89e453af6a`는 **로컬 실행**이며 현장 전체 자료의 통계가 아니다.

- CNN 후보 30회 중 1회가 1,000 epoch 상한에 도달했다. `per_segment_z`, width=8, seed=43이다. 최고점은 861 epoch이고 끝까지 139 epoch 동안 개선되지 않아 patience=150을 채우기 11 epoch 전에 종료됐다. 연장 확인 후보지만, 현재 숫자만으로 계속 개선 중이라고 말할 수 없다.
- CNN 단계는 약 42분 12초, 실험 A는 약 2분 47초였다. 비용 판단에는 모델별 학습 시간과 평가·입출력 시간까지 구별해야 한다. 이 측정을 현장 전량 처리 시간으로 그대로 외삽하지 않는다.
- 사용 가능한 인자 28개 중 1개는 상수, 1개는 구조적으로 관측 불가능 상태였다. 값이 0인 것과 현재 창/인자 정의로 측정할 수 없는 것을 구별하는 것이 추가 모델 탐색보다 먼저다.
- 라벨만 있고 연결된 annotation이 없는 기록 35건이 기록돼 있다. 입력 묶음 차이인지 실제 누락인지 확인해야 하며, 분석 표본 수만 보고 완전한 코호트라고 해석하면 안 된다.

## 4. 두 번 방문할 때 평가 설계를 보호하는 방법

첫 방문의 결과를 보고 두 번째 실험을 설계하면 첫 방문 결과는 탐색 근거다. 같은 holdout을 계속 보면서 변경한 모델의 향상을 독립 확증으로 해석할 수는 없다. 가능하면 두 번째 방문의 최종 평가에 사용할 산모/기관/기간을 사전에 잠그고, 개발은 분리된 코호트에서 한다. 새 평가 자료가 없다면 독립 검증의 한계를 명시한다.

현재 `--prior-cohort`는 이미 본 산모를 주 holdout의 train에만 넣는 수단이다. CV·LOSO·아웃컴 분석까지 모두 미관측 자료로 바꿔주는 기능이 아니며, 동일한 전체 자료를 모두 prior로 지정하면 새 holdout을 확보할 수 없다. 퇴실 전에 어떤 평가를 이미 열람했는지와 다음 평가 대상의 확보 가능성을 결정해야 한다.

## 5. 개발 우선순위와 방문 순서

**P0 — 다음 방문 설계를 좌우하는 관측 확보**: 학습 전 구조 조사, 파싱/제외 흐름, 라벨·시각·ID 의미 확인, 일지 파일 저장, CNN/트리 중단 이유 진단, 내부 결과 보존/재개 확인. 현재 저장된 CNN history와 데이터 요약부터 재사용한다.

**P1 — 관측된 문제를 구별하는 작은 실험**: 문제가 있는 후보의 예산 연장, 산모 수 learning curve, 기관별 품질·결측 차이, 개발 코호트의 오류 유형 표본 검토. 모든 후보를 더 크게/더 오래 돌리는 대신 어떤 가설을 판별할지 사전에 정한다.

**P2 — 입력과 라벨의 의미가 확인된 뒤의 변경**: 시간 문맥, 추가 변수, 새 모델군, 재보정/기관별 정책. 제약이 모델인지 자료인지 판단한 뒤 선택한다.

첫 방문은 `구조 조사 → 기존 실험 → 품질/학습/비용 진단 → 다음 방문 의사결정표 → 결과 보존·재개 점검` 순서로 운영한다. 조사 결과에 맞는 패키지 수정과 사전 테스트를 2주 동안 준비한다. 두 번째 방문은 입력·환경 변화 확인 후 계획한 비교를 실행하고, 마지막에 잠가둔 평가를 수행한다.

의사결정표 예시:

| 관측 | 다음 비교 | 결정 기준 |
|---|---|---|
| 상한에 도달했고 검증 이력상 종료 판단이 미완료 | 해당 설정의 사전 정의된 추가 예산 | validation 개선의 크기·seed 안정성과 추가 비용 |
| 개발 데이터 learning curve가 계속 상승 | 더 많은 산모 확보 또는 표본 범위 확대 | 산모 수 증가당 개선과 기관 분포 변화 |
| 특정 기관에서 결측/평탄 신호가 집중 | 품질 원인 확인 후 개발 코호트에서 처리 방법 비교 | 사용 가능 표본, 누수 없는 성능, 경보 부담 |
| train은 개선되지만 validation은 악화 | 용량/정규화 한 요소씩 비교 | 개발 검증의 성능·안정성; test로 선택하지 않음 |
| 시간 연결은 파일명으로만 추정 가능 | 실제 시각·기록 연결 의미를 기관에 확인 | 확인 전까지 연속 시간 맥락의 임상적 의미를 확정하지 않음 |
